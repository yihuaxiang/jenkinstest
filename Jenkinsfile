pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'echo "hello world"'
                sh 'pwd'
                sh 'ls -al'
            }
        }
    }
}
